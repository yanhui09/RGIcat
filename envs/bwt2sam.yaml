# conda dependence for bowtie2 (libtbb.so.2) and samtools
name: bwt2-samtools
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bowtie2 ==2.4.1  # Keep consistent with version specified in bowtie2/align
  - tbb ==2020.2
  - samtools ==1.10
  - perl